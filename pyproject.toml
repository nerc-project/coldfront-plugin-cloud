[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.ruff.lint.isort]
known-first-party = ["coldfront-plugin-cloud"]
force-single-line = false

[tool.ruff.format]
quote-style = "double"
docstring-code-format = true
